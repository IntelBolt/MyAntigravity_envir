# Инструкция по подключению нового клиента к IntelBolt Analytics (v1.1)

Этот чек-лист поможет вам быстро и безошибочно развернуть систему сквозной аналитики для нового клиента. 

---

## 📋 Шаг 1: Сбор ресурсов и секретов
Соберите данные из кабинетов клиента:

### Google Analytics 4 (GA4)
*   **Measurement ID:** Администратор -> Потоки данных -> Ваш поток -> Идентификатор потока (G-XXXXXXX).
*   **API Secret:** Потоки данных -> Секретные ключи API Measurement Protocol -> Создать (IntelBolt).
*   **Property ID:** Администратор -> Параметры ресурса -> Идентификатор ресурса.

### Google Cloud Platform (GCP)
*   **Включение API:** В разделе "Library" найдите и включите **Google Analytics Reporting API** и **Google Drive API**.
*   **API Key:** Credentials -> Create Credentials -> API Key.
*   **OAuth:** Credentials -> Create Credentials -> OAuth Client ID (Web Application).

---

## 📊 Шаг 2: Конфигурация (Google Sheets)
1.  Добавьте строку в мастер-таблицу (Лист конфигурации).
2.  Запустите воркфлоу генерации Client ID. 
3.  **Важно:** Убедитесь, что 5-значный ID заполнился и синхронизировался в БД Postgres (`client_credentials`).

---

## ⚡ Шаг 3: Настройка n8n (Backend)
> **Про Client ID:** Обработка множества клиентов уже автоматизирована. Воркфлоу перебирают всех клиентов из базы. Вам **не нужно** вручную менять `WHERE client_id = ...` в SQL-узлах.

**Что НУЖНО сделать:**
1.  В n8n обновите Credentials (CRM, GA4) для нового клиента.
2.  В узле "Преобразование данных" (в воркфлоу CRM) обновите **ID кастомных полей** (UTM и google_client_id), так как они уникальны для каждого аккаунта AmoCRM.

---

## 🌐 Шаг 4: Настройка GA4 Custom Dimension
Чтобы GA4 понимал ID посетителя, передаваемый из CRM:
1.  **Администратор -> Отображение данных -> Специальные определения**.
2.  Создайте параметр:
    *   Название: `google_client_id`
    *   Область: Событие (Event)
    *   Параметр: `google_client_id`

---

## 💻 Шаг 5: UTM-метки
Все рекламные ссылки должны содержать:
*   `utm_source` (источник)
*   `utm_medium` (тип)
*   `utm_campaign` (кампания)
Пример: `site.com/?utm_source=yandex&utm_medium=cpc&utm_campaign=sale`

---

## 🏢 Шаг 6: Сайт и Формы
1.  **CRM:** Создайте текстовые поля для CID и UTM. Запишите их ID.
    > **💡 Лайфхак (как найти ID):** 
    > * Способ 1: В n8n запустите узел "Get Lead Info". В JSON-ответе в массиве `custom_fields_values` вы увидите связку `field_id` и названия.
    > * Способ 2: В AmoCRM зайдите в Настройки -> Поля. Кликните на поле — его ID будет в конце ссылки в браузере.
2.  **Формы:** Добавьте скрытые поля `<input type="hidden" name="google_client_id">` и для UTM.
3.  **Скрипт:** Установите `SITE_CAPTURE_SCRIPT.js` на все страницы.

---

## 🚀 Шаг 7: Деплой Дашборда
В настройках Vercel (Environment Variables) укажите:
*   `POSTGRES_URL`
*   `NEXT_PUBLIC_CLIENT_ID` (5-значный номер клиента)
*   `NEXT_PUBLIC_N8N_WEBHOOK_URL`

---
*Документация обновлена 24.02.2026*
