# Журнал проекта IntelBolt

## Статус на 2026-02-10 14:14
Проект успешно деплоен на Vercel (https://my-antigravity-envir.vercel.app/). Идет этап настройки интеграций и очистки структуры.

## Что сделано:
1.  **Очистка структуры**: 
    - Удалена дублирующая папка `dashboard/`. Теперь основным является корень проекта. 
    - Исправлены конфликты (git merge conflicts) в файлах `ME.md`, `README.md`, `agents.md` и `.gitignore`.
2.  **Анализ n8n**:
    - Изучены существующие воркфлоу (CRM, GA4, Reporter). Найдена база данных Postgres на VPS (`51.222.30.72`).
3.  **Подготовка к подключению Агента**:
    - Определен план интеграции чата дашборда с n8n через Webhook.

## Следующие шаги:
1.  **Настройка Переменных Окружения в Vercel**:
    - Нужно добавить `NEXT_PUBLIC_N8N_WEBHOOK_URL` (URL вебхука от твоего AI Агента в n8n). Это позволит запросам из браузера уходить сразу на твой VPS.
2.  **Обновление AIChat.tsx**: 
    - Изменение логики отправки сообщений, чтобы вместо моков данные летели в n8n.
3.  **Подключение Графиков к БД**:
    - Создание API эндпоинтов в Next.js для получения реальных цифр из Postgres.

## Секреты для Vercel (Список для добавления):
- `POSTGRES_URL` (или отдельные хост/юзер/пароль для прямого доступа к аналитике).
- `NEXT_PUBLIC_N8N_WEBHOOK_URL` (для чата).

---

## Статус на 2026-02-10 22:20 (План на завтра)
Проект синхронизирован на съемный носитель (H:\Antigravity). Локальный сервер работает, ошибки типов исправлены.

### 📝 Инструкция по переезду на рабочий компьютер:
1. **Копирование**: Перенеси папку `Antigravity` со съемного диска на диск `C:\` рабочего компьютера.
2. **Установка зависимостей**: Открой терминал в корне проекта и выполни:
   ```powershell
   npm install
   ```
3. **Запуск сервера**: Запускай проект командой:
   ```powershell
   npx next dev
   ```
   *Примечание: Если `npm run dev` выдает ошибку, используй только `npx next dev`.*

### 🛠️ Технический долг (Agent & n8n):
- **Проблема**: Нода `Postgres Chat Memory` в n8n падает с ошибкой при запросах из Дашборда.
- **Причина**: В поле `Key` ожидается только число (ID Telegram), а Дашборд шлет строку `dashboard_session_1`. Также колонка в БД может иметь тип INTEGER вместо TEXT.
- **Что сделать**: 
    1. В n8n в ноде памяти заменить Key на: `={{ $json.chatId || $node["Telegram Trigger"]?.json?.message?.chat?.id || "dashboard_user" }}`.
    2. Если ошибка в БД останется, выполнить SQL: `ALTER TABLE n8n_chat_histories ALTER COLUMN session_id TYPE TEXT;`.

### ✅ Что уже сделано сегодня:
- Исправлены ошибки типов в `page.tsx`, `AIChat.tsx`, `input.tsx`.
- Проект успешно деплоится на Vercel (деплой обновлен через принудительный пуш).
- Код синхронизирован на диск H:.

---

## Статус на 2026-02-12 19:15

### ✅ Выполнено (Критические исправления):
- **Связь с n8n (CORS & Connectivity)**: Полностью решена проблема "мгновенной ошибки" при отправке сообщений. 
    - Создан **API Route Handler** (`app/api/chat/route.ts`), который выступает прокси-сервером. 
    - Браузер теперь общается с `/api/chat`, а сервер Next.js пересылает запросы в n8n Сервер-Сервер. Это исключило любые проблемы с CORS и упростило отладку.
- **Фикс 404 в n8n**: Выявлено, что n8n возвращал 404 при определенных типах заголовков. Возврат к стандартному `application/json` через прокси решил проблему.

### 🎨 Интерфейс и UX (Новые возможности):
- **Collapsible Sidebar**: Добавлена возможность сворачивать левое меню. Теперь дашборд и чат могут занимать всё полезное пространство.
- **Исправление AIChat**:
    - Полностью переделана логика прокрутки. Сообщения теперь скроллятся внутри контейнера, а поле ввода жестко зафиксировано внизу.
    - Добавлен автоматический скролл к последнему сообщению при ответе Агента.
    - Добавлена поддержка многострочного текста (`whitespace-pre-wrap`), теперь отчеты Агента выглядят аккуратно.
- **Стилизация**: Внедрены кастомные тонкие скроллбары (`custom-scrollbar`) для всех панелей через `globals.css`.

### 📂 Работа с документами:
- Подключились и проанализировали документ из Google Drive с описанием ошибок. Вся информация из документа была учтена при исправлении работы вебхуков.

### 📅 План на следующую сессию:
1. Проверить работу Агента с реальными данными из БД (AmoCRM/GA4).
2. Настроить автоматическое переключение на Production Webhook URL в `.env.local` после завершения всех тестов.
3. Добавить индикатор "печатает..." (typing indicator) для более живого общения.

---
## Статус на 2026-02-13 16:35

### ✅ Выполнено:
- **Redesign Overview**: Главная страница полностью обновлена. Внедрены KPI: Total Won, Total Lost, Win Rate, Marketing Traffic.
- **Performance Chart**: Создан новый компонент графика, отображающий динамику Won/Lost сделок.
- **Premium UI**: Обновлены карточки индикаторов (KPICard) с использованием градиентов, иконок и эффекта стекла.

### 🚀 Долгосрочное видение (Масштабирование):
1.  **Мультивалютность**: Вынести символ валюты в настройки/env. CRM отдает только числа, форматирование (₽, ₸, $) должно быть динамическим.
2.  **Архитектура подключения**: 
    - Для стабильных аналитических данных использовать **прямой API эндпоинт** (Next.js -> Postgres) — это быстрее и надежнее.
    - n8n использовать для чата и гибких отчетов, где логика может часто меняться.
3.  **Client Onboarding (SaaS-подход)**: 
    - Разработать систему авторизации через OAuth (GA4, AmoCRM, Bitrix24), чтобы клиент подключался "в один клик" без ручного копирования ключей.
    - Поддержка Bitrix24: Создать альтернативные воркфлоу в n8n для маппинга данных Bitrix24 в единую структуру аналитики.
    - Использовать одну кодовую базу (SaaS), где данные разделяются по `client_id` или поддомену.
4.  **Хостинг**: 
    - Фронтенд: Vercel. 
    - Вычисления и БД: Твой VPS (Docker-контейнеры под каждого крупного клиента или общая база с разделением прав).

### 📅 План на следующую сессию:
1. Создать API-ендпоинт `/api/stats`, который будет забирать реальные данные из БД `marketing_data` и `view_managers_performance`.
2. Настроить фронтенд на получение данных из этого API вместо моков.
3. Исправить валюту на динамическую переменную.

---
*Изменения зафиксированы. Проект готов к фазе наполнения данными.*

---
## Статус на 2026-02-16 17:10

### ✅ Выполнено (Масштабное обновление дашборда):

1.  **Создание многостраничной структуры**:
    *   Реализованы три выделенные страницы аналитики: **Маркетинг (GA4)**, **CRM & Продажи**, **Сквозная аналитика (Identity Reports)**.
    *   Каждая страница подключена к своим источникам данных в Postgres (`marketing_data`, `leads_data`, `view_full_identity_report`).
    *   Внедрена плавная навигация с подсветкой активных пунктов меню.

2.  **Унифицированный компонент заголовка (DashboardHeader)**:
    *   Создан переиспользуемый заголовок с поддержкой динамических тегов, заголовков и подзаголовков.
    *   **Календарь**: Реализован выбор диапазона дат с исправленным Z-index (теперь выпадает поверх всех элементов).
    *   **Функционал "Поделиться"**: После тестов скриншотов и PDF было принято решение перейти на более надежный метод **"Копировать ссылку"**. Это исключает ошибки рендеринга современных CSS-цветов (`oklch`) и дает максимальную гибкость.

3.  **Инфраструктура для AI-Аналитики**:
    *   В базе данных созданы специальные представления (Views):
        - `view_weekly_performance`: Рассчитывает Week-over-Week (WoW) изменения сессий, дохода и сделок.
        - `view_ai_summary`: Подготавливает упрощенный срез данных для быстрого анализа AI-агентом.
    *   Это решило проблему Агента с "отслеживанием изменений во времени" — теперь он просто запрашивает готовые расчеты из БД.

4.  **Оптимизация и Cleanup**:
    *   Удалены неиспользуемые тяжелые зависимости (`html2canvas`, `jspdf`, `html-to-image`).
    *   Удалены временные API-роуты для шаринга картинок, проект стал легче и чище.
    *   Интерфейс переведен на русский язык для консистентности.

### 🛠️ Технические детали:
- Стили приведены в соответствие с **Tailwind CSS 4**.
- Добавлены микро-анимации (`animate-in`, `fade-in`, `slide-in`) для всех новых страниц.
- Данные для графиков (`PerformanceChart`) теперь полностью динамические.

### 📅 План на следующую сессию:
1.  **Связь Календаря с API**: Настроить передачу выбранных в заголовке дат в SQL-запросы всех страниц для фильтрации данных "на лету".
2.  **Авторизация**: Добавить простую защиту страниц (Auth), если проект готовится к публичному доступу.
3.  **Мобильная адаптация**: Проверить и подправить отображение графиков на смартфонах.

---
*Изменения зафиксированы. Дашборд превратился из одностраничного прототипа в полноценный BI-инструмент.*


---
## Статус на 2026-02-21 12:35

### ✅ Выполнено (Устранение ограничений данных для Агента):

1. **Снятие фильтров в базе данных**:
   - **`final_analytics_report`**: Удален жесткий фильтр `WHERE client_name = 'Тест'`. Теперь представление объединяет данные всех клиентов, что критично для сквозной аналитики.
   - **`view_ai_summary`**: Удалено ограничение `LIMIT 5`. Теперь Агент видит всю историю еженедельной динамики, а не только последние 5 недель.
2. **Анализ актуальности данных**:
   - Проведена полная диагностика таблиц `marketing_data`, `leads_data` и `calls_data`.
   - Установлено, что последние записи в базе датируются **28.01.2026**. Это объясняет, почему Агент сообщал об "ограничении" до 25 января — он видел последнюю полную неделю. В феврале 2026 года данных в базе пока нет.
3. **Аудит n8n воркфлоу**:
   - Обнаружено ограничение в воркфлоу `BI analytics _данные из СРМ` (ID: `AdX27yRNTfIQUhAF`). В узле получения данных из Postgres стоит фильтр только по одному клиенту.
   - Даны рекомендации по замене на `SELECT * FROM client_credentials;` для обработки всех активных аккаунтов.
4. **Документирование**:
   - Создан эталонный файл `final_analytics_report.sql` для фиксации структуры главного представления без фильтров.
   - Обновлен файл `view_ai_summary.sql`.

### 🛠️ Технические правки:
- Очищены временные диагностические скрипты из корня проекта.
- Проверена структура таблицы `client_credentials` — в ней на данный момент всего один клиент ("Тест"), что подтверждает необходимость настройки CRM-синхронизации для других клиентов.

### 📅 План на следующую сессию:
1. Исправить SQL-запросы в n8n (узел "Достаём данные клиентов").
2. Проверить статус импорта данных за февраль из GA4 и AmoCRM.
3. Скорректировать описания инструментов (AI Tools) в n8n, чтобы Агент не путал таблицы.

---
*Изменения внесены. База данных готова к приему реальных данных, ограничения для ИИ сняты.*
## Статус на 2026-02-19 13:50

### ✅ ыполнено (Стабилизация и LIVE-режим):

1. **ткат к стабильной версии**: роизведен Hard Reset до коммита `366951f` (16 февраля). то позволило избавиться от нестабильных зависимостей и конфликтов версий Next.js/React, возникших при попытке внедрить сложный календарь.
2. **прощение интерфейса даты**:
   - бран интерактивный выбор диапазонов дат.
   - ашборд переведен в режим "Сегодняшний день". 
   -  заголовке (`DashboardHeader`) теперь отображается только текущая дата.
3. **втоматизация и LIVE-обновление**:
   - а все страницы аналитики (Overview, Marketing, Sales, Identity) добавлен хук `setInterval`.
   - анные теперь автоматически обновляются каждые **30 минут** без перезагрузки страницы.
   -  API-роуты добавлена директива `revalidate = 0`, обеспечивающая получение свежих данных из Postgres при каждом запросе.
4. **ильтрация по дате**:
   - SQL-запросы в API перенастроены на забор данных за текущий день (`CURRENT_DATE`), где это поддерживается схемой .

### 📊 Текущее состояние:
- роект работает стабильно.
- нтерфейс фокусно показывает результаты "здесь и сейчас".
- ыполнен коммит `39d7842` с финальными правками.

---

---

## Статус на 2026-02-22 21:15

### ✅ Выполнено (Анализ масштабируемости и SaaS-архитектуры):

1. **Концепция "Один сервер - Много клиентов"**:
   - Принято решение использовать единую базу данных и один экземпляр n8n для всех заказчиков.
   - Изоляция данных будет реализована через `client_name` (или `client_id`) во всех таблицах и представлениях.

2. **Аудит текущей схемы БД**:
   - Подтверждено наличие колонки `client_name` в основных таблицах: `marketing_data`, `leads_data`, `calls_data`.
   - Выявлена необходимость доработки SQL Views (`final_analytics_report`, `view_ai_summary`), так как сейчас они "смешивают" данные разных клиентов.

3. **План Onboarding-процесса**:
   - Составлен чек-лист шагов для настройки нового клиента (Credentials -> n8n Workflows -> DB Provisioning -> Dashboard Deploy).

### 🛠️ План на завтра (Масштабирование):

1. **Рефакторинг SQL Views**:
   - Добавить `client_name` во все `SELECT` и `JOIN` условия во всех представлениях.
   - Обновить `view_weekly_performance`, чтобы расчеты шли в разрезе каждого клиента.

2. **Изоляция в n8n**:
   - Настроить передачу `client_name` из воркфлоу в БД при каждой записи.
   - Добавить обязательную фильтрацию по клиенту в инструментах ИИ-Агента (SQL Tool).

3. **Безопасность чата**:
   - Обновить таблицу `n8n_chat_histories`, добавив привязку сессии к конкретному клиенту.

4. **Дашборд**:
   - Настроить API-роуты Next.js на фильтрацию данных по клиенту (через заголовок или параметры запроса).



---
## Статус на 2026-02-23 14:45

### ✅ ИТОГИ ДНЯ (Глобальный переход на Unique Client IDs):

Сегодня была проведена фундаментальная работа по превращению дашборда из "одноклиентского" в полноценную SaaS-систему с изоляцией данных.

#### 1. База данных (Полный рефакторинг):
- **Схема**: Таблицы `client_credentials`, `marketing_data`, `leads_data`, `calls_data` и `managers_data` теперь содержат колонку `client_id` (INTEGER).
- **Миграция**: Клиенту "Тест" присвоен эталонный ID `10234`. Все исторические данные успешно перепривязаны к нему.
- **Views**: Все представления переписаны. Теперь они поддерживают фильтрацию по ID. Динамика в `view_weekly_performance` теперь считается для каждого клиента независимо (Partitioning).

#### 2. Автоматизация в n8n:
- **Генерация ID**: Внедрен механизм создания случайных 5-значных чисел при регистрации нового клиента.
- **Синхронизация CRM & GA4**: Воркфлоу обновлены. Теперь они корректно прокидывают `client_id` через все узлы. Ошибки синтаксиса в SQL устранены.

#### 3. API и Дашборд (Финальный апдейт):
- **Фильтрация**: Все API-роуты (`stats`, `marketing`, `sales`, `identity`) переписаны с поддержкой параметра `clientId`.
- **Backward Compatibility**: Сохранена работа с тестовым клиентом по умолчанию (`10234`).
- **Бизнес-логика**: Все агрегации (SUM/COUNT) теперь изолированы на уровне БД.

### 🏁 Текущее состояние:
- Проект **полностью готов** к запуску в мультиклиентском режиме. 
- Система стабильна, данные синхронизируются корректно.

---

---
## Статус на 2026-02-24 18:25

### 🎯 Новая стратегия: Универсальная Платформа (Marketplace Agnostic)

Принято стратегическое решение развивать систему как универсальный хаб для любых маркетплейсов, а не решение под конкретный API. Это обеспечивает гибкость для конечного заказчика.

#### ✅ Выбрано для теста: Ozon Seller
- **Почему**: Бесплатная регистрация, отсутствие абонентской платы (комиссия только с продаж), современный и документированный API.
- **Цель**: На базе Ozon отработать архитектуру "Адаптера", которая в будущем позволит подключать Wildberries, Яндекс Маркет или Amazon простым добавлением нового модуля в n8n без переделки дашборда.

#### 🏗️ Архитектурные принципы:
1.  **Обязательная фильтрация `client_id`**: Подтверждено жесткое правило — любая новая таблица (заказы, остатки, транзакции) ДОЛЖНА содержать колонку `client_id` для обеспечения изоляции данных в SaaS-модели.
2.  **Паттерн "Адаптер"**:
    - **n8n**: Будет выступать в роли "Переводчика" (Mapper), который приводит специфические ответы API Озона к единому формату IntelBolt.
    - **БД**: Использование унифицированных таблиц (например, `universal_orders`), где поле `source` определяет происхождение данных.
3.  **Мультиклиентность**: Все настройки API-ключей для разных площадок будут храниться в расширенной таблице `client_credentials`, привязанной к `client_id`.

### 📅 План на завтра (Утро):
1.  Настройка интеграции с Ozon в n8n.
2.  Создание первой "универсальной" таблицы заказов с поддержкой `client_id`.
3.  Первый тестовый запуск получения данных.

---
## Статус на 2026-02-25 11:55

### ✅ Выполнено:
1. **Смена вектора на Shopify**: В связи с ограничениями Ozon, выбран Shopify (Dev Store) в качестве первого маркетплейса для тестирования. Это полностью соответствует цели создания "Универсальной платформы".
2. **Масштабирование БД**:
   - Таблица `client_credentials` расширена полями для поддержки маркетплейсов (`shop_url`, `shop_api_key`, `shop_api_secret`, `shop_id`, `shop_type`). Это позволяет хранить настройки для Shopify, Ozon, WB и др.
   - Создана универсальная таблица `sales_data` для хранения заказов из любых источников с привязкой к `client_id` и `source`.
3. **Подготовка n8n**: Сформулирован обновленный SQL-запрос для воркфлоу загрузки конфигурации.

### 🛠️ Технические детали:
- Новая таблица `sales_data` включает: `total_price`, `currency`, `customer_name`, `status`.
- Добавлен индекс `idx_sales_client_id` для быстрой фильтрации по клиентам.

### 📅 План на следующую сессию:
1. Настроить воркфлоу в n8n для Shopify (используя полученный API токен).
2. Загрузить первые тестовые заказы из Shopify в `sales_data`.
3. Обновить API дашборда для вывода данных из `sales_data`.

---
## Статус на 2026-02-25 15:50

### ✅ Выполнено:
1. **Интеграция Shopify (Конфигурация)**: Полный успех. Все креды (URL, API Key, Type) успешно передаются из Google Таблиц в PostgreSQL через n8n.
2. **Тестовые данные**: В Shopify Dev Store создан первый тестовый заказ на сумму $600. Магазин переведен в боевой режим для покупателей.
3. **Подготовка Инфраструктуры**: База данных готова к приему данных о продажах. Таблица `sales_data` ждет первой синхронизации.

### 🛠️ Технические детали:
- Исправлен SQL в n8n для поддержки новых полей (защита от `undefined`, маппинг `shop_` полей).
- Подтверждена работа Apps Script с расширенным набором данных.

### 📅 План:
1. Дождаться завершения настройки воркфлоу синхронизации заказов.
2. Проверить появление данных в `sales_data`.
3. Начать разработку API-роутов для вывода статистики продаж на дашборд.

---
## Статус на 2026-02-25 16:50

### ✅ Выполнено:
1. **Расширение Аналитики (БД)**: Таблица `sales_data` дополнена полями для глубокого анализа:
   - `customer_email`: Основной ключ для сквозной аналитики.
   - `order_number`: Форматированный номер заказа (напр. #1001).
   - `source_customer_id`: Оригинальный ID покупателя для расчета LTV.
   - `landing_site`: Данные о пути клиента и UTM-метках.
2. **Миграция**: Успешно применен скрипт `expand_sales_data.js`.
3. **Безопасность данных**: Все новые поля сделаны необязательными, что обеспечивает совместимость с маркетплейсами, отдающими меньше данных.

### 🛠️ Техническая Сводка:
- Реализован паттерн "Заглушка" в SQL-запросах (использование ID клиента вместо имени при его отсутствии).
- Успешно протестирован сбор данных Shopify в режиме "Split Out".

---
## ИТОГИ ДНЯ: 2026-02-25 17:00 — Shopify E2E Integration Success 🚀

### 🏆 Главное достижение:
Реализован полный цикл (End-to-End) интеграции маркетплейса. Данные из Shopify успешно попадают в нашу централизованную БД в автоматическом режиме.

### ✅ Что сделано за сегодня:
1. **Pipeline Конфигурации**: Исправлена цепочка Google Sheets -> n8n -> Postgres. Теперь любые данные маркетплейсов (API ключи, URL) сохраняются без потерь.
2. **ETL-процесс для Продаж**: Самостоятельно (с поддержкой агента) настроен воркфлоу в n8n, который выкачивает заказы из Shopify.
3. **Аналитическое расширение БД**: Таблица `sales_data` теперь умеет хранить Email, Номер заказа, ID клиента и даже `landing_site` для будущего маркетингового анализа.
4. **Первая "Кровь"**: Успешно синхронизирован реальный тестовый заказ на **$600**. Система подтвердила работу в мульти-клиентском режиме (SaaS).

### 📋 Состояние системы:
- `client_credentials`: Содержит актуальные Shopify токены.
- `sales_data`: Содержит данные о первом заказе.
- `n8n`: Оба воркфлоу (Config и Shopify Sync) активны.

### 📅 План на завтра:
1. **Дашборд**: Интегрировать `sales_data` в API роуты фронтенда. Вывести сумму продаж Shopify на графики.
2. **Attribution**: Начать работу над парсингом `landing_site` для вытаскивания UTM-меток прямо из Shopify заказа.

---
*День завершен на позитивной ноте. Система готова к масштабированию на другие маркетплейсы.*
---
## Статус на 2026-02-26 13:40

### ✅ Выполнено (Marketplaces Dashboard & Multi-Currency):

1. **Новый раздел "Marketplaces"**:
   - В боковое меню добавлен раздел **Marketplaces Dashboard**.
   - Создана выделенная страница аналитики с премиальным дизайном на базе Recharts.
   - Реализована универсальная страница, объединяющая данные со всех подключенных площадок (Ozon, WB, Shopify, Kaspi).

2. **Мультивалютность и живая конвертация**:
   - Реализован механизм автоматической конвертации всех сумм в **KZT (тенге)** для верхних KPI и общих графиков.
   - Интегрирован API курсов валют (через `exchangerate-api.com`) для расчета в реальном времени (USD, RUB, CNY -> KZT).
   - В таблице заказов сохранено отображение в **оригинальной валюте** платформы ($, ₽, ₸) для удобства сверки.

3. **Архитектура Multi-Marketplace**:
   - Создана новая таблица `marketplace_credentials`. Теперь один клиент (по `client_id`) может подключить **неограниченное количество аккаунтов** любых маркетплейсов одновременно.
   - Проведена миграция существующих тестовых данных Shopify в новую структуру.

4. **Интерактивность и фильтры**:
   - Добавлен фильтр по площадкам, позволяющий мгновенно переключать весь дашборд между "Всеми данными" и конкретным магазином.
   - Добавлена продвинутая круговая диаграмма (**Platform Performance Share**) с детализацией оборота, количеству заказов и вкладу каждой платформы в общий бюджет.

### 🛠️ Технические детали:
- API `/api/marketplaces` теперь возвращает как отформатированные строки для UI, так и `raw` числа для точных расчетов на фронтенде.
- Исправлено поведение `KPICard`, добавлена поддержка нейтрального тренда (для статичных показателей).
- Оптимизирована база данных: добавлены индексы и уникальные ключи для предотвращения дублей при работе с несколькими источниками.

### 📅 План на следующую сессию:
1. **Симуляция данных**: Принять решение о создании тестовых данных для Ozon/WB (физлица не могут создать реальное API).
2. **WooCommerce**: Изучить возможность подключения тестового магазина на WordPress как альтернативу закрытым маркетплейсам.
3. **Attribution 2.0**: Продолжить работу над UTM-метками внутри заказов маркетплейсов.

---
## Статус на 2026-02-27 17:15

### ✅ Выполнено (BigCommerce Integration & System Stabilization):

1.  **Интеграция BigCommerce**:
    *   В таблицу `marketplace_credentials` успешно добавлены данные нового магазина (Store Hash: `aftam5islz`).
    *   Создан и протестирован воркфлоу в n8n на базе Shopify-шаблона. Настроен маппинг полей BigCommerce API (v2) в нашу универсальную схему `sales_data`.
    *   Добавлен первый реальный заказ ($450) из BigCommerce dev-store. Проверено отображение на дашборде.

2.  **Стабилизация системы (Борьба с "уплывающими" данными)**:
    *   **Очистка БД**: Удалены некорректные строки (`undefined`) из `client_credentials`, которые блокировали работу CRM и GA4 выгрузок.
    *   **Apps Script 2.0**: Код в Google Таблице переписан с использованием "безопасного чтения" (метод `getVal`), исключающего отправку пустых строк.
    *   **n8n Protection**: В воркфлоу загрузки конфигурации внедрена нода-фильтр, блокирующая запись в БД, если имя клиента пустое.
    *   **SQL Robustness**: Рекомендовано использовать фильтрацию `IS NOT NULL` в узлах чтения данных, чтобы защититься от случайного "мусора".

3.  **Диагностика GA4**:
    *   Установлено, что `ga_id` у клиента "Тест" в данный момент равен `NULL`. Это требует обновления данных через Google Таблицу (ячейка B14) для корректной работы воркфлоу GA4.

### 🛠️ Технические правки:
- Исправлена ошибка `item is not defined` в n8n (переход на `$json`).
- Исправлен `order_number` (удален лишний префикс `##`).
- Проверена целостность индексов `sales_data` для предотвращения дублей.

### 📅 План на следующую сессию:
1.  Обновить `ga_id` через таблицу и проверить запуск воркфлоу «Данные из GA4».
2.  Настроить автоматическое расписание (Cron) для BigCommerce Sync в n8n.
3.  Начать финальное тестирование сквозной аналитики на живых данных (AmoCRM + BigCommerce + GA4).

---
*Изменения зафиксированы. Система стабилизирована, многоканальность подтверждена на практике.*

